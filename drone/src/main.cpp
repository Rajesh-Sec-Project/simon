#include <iostream>
#include <string>
#include <vector>

#include "lconf/json.h"

int main() {
    /*using namespace lconf;

    int a;
    std::vector<int> b;

    json::Template tpl;
    tpl.bind("a", a).bind("b", b);*/

    return 0;
}
